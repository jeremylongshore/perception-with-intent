# Archived GitHub Workflows

These workflows belong to the pre-repurpose setup (multi-env deployment, ingestion, Slack webhook).  
They are parked here while the repository transitions to the minimal IAM JVP Base baseline.  
Reintroduce components selectively after the new CI/CD plan is approved.
